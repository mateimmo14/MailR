[project]
name = "MailR"
version = "0.1.0"
description = "A CLI email viewer/sender"
requires-python = ">=3.14"
dependencies = [
    "inquirer>=3.4.1",
    "pillow>=12.1.1",
    "pyinstaller>=6.19.0",
]

